import React, { Component } from 'react';
import dada from "../../../data/shishiData.json"

class System extends Component {
    state = {}
    render() {
        return (
            <div>system</div>
        );
    }
}

export default System;